import React from 'react';
import { StyContainer } from './styes';

/**
 * @description DashboardCircleSpinner Component.
 * @param {number} size Spinner size.
 */
const DashboardCircleSpinner: React.FC<{}> = ({ }) => {
 return <StyContainer />;
};

export default DashboardCircleSpinner;
